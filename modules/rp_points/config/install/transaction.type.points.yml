langcode: en
status: true
dependencies:
  enforced:
      module:
        - rp_points
id: points
label: Points
target_entity_type: user
transactor:
  id: userpoints
  settings:
    roles: ''
    amount: field_points_amount
    balance: field_points_balance
    log_message: field_points_reason
    target_balance: field_points_points
bundles: {  }
options: {  }
